#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
    float a=342.232;
    int celling=ceil(a);
    int flooring=floor(a);
    
    int s=sqrt(a);
    float power=pow(s,3);
    int b=-232;
    int absulate=abs(b);
    printf("%d",absulate);
    return 0;
}